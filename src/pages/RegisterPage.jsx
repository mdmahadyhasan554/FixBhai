import RegisterForm from '../features/auth/RegisterForm'

/**
 * RegisterPage
 * Renders RegisterForm centred on the page. Contains zero UI markup.
 */
const RegisterPage = () => (
  <div className="container py-5">
    <RegisterForm />
  </div>
)

export default RegisterPage
